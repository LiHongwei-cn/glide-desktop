# Glide 隐私说明

## 结论

Glide 不建立云端用户档案，不包含远程分析 SDK，也不会把启动事件、设备标识、诊断结果或线路配置发送给 Glide 服务器。

## 本机保存的数据

| 数据 | 保存位置 | 用途 |
| --- | --- | --- |
| 管理密码、订阅地址 | macOS Keychain / Windows Credential Manager | 连接与设备导入 |
| 线路名称、管理地址、地区偏好 | 本地 SQLite | 展示与本机检查 |
| 启动次数、活跃日期、设备数量 | 本地 SQLite | 向当前用户展示本机使用情况 |
| 脱敏诊断结果 | 本地 SQLite | 排查连接问题 |

本机统计最多保留 365 个活跃日期，不包含全局用户 ID，可在“设置”中重置。诊断默认保留 7 天，可改为 14 或 30 天，也可立即清除。

## 会发生的网络请求

- 用户主动检查连接时，请求用户填写的 HTTPS 管理地址；
- 系统 DNS 使用标准 Fake-IP 时，Glide 只沿用本机代理映射，并继续验证原始域名
  的 HTTPS 证书；不会把管理域名发送给额外的公共 DNS API；
- 用户点击官方下载按钮时，由系统浏览器打开允许列表中的 GitHub 或 Cloudflare 官方页面；
- 当前版本不向 Glide 自有服务器发送请求。

GitHub、Cloudflare、操作系统和网络服务商可能按照各自政策处理网络元数据，这些处理不由 Glide 控制。

## 不保存的数据

- Cloudflare 账号密码；
- Global API Key；
- 用户浏览历史和访问内容；
- 远程遥测标识；
- 完整订阅地址或密码的诊断日志。

## 用户控制

用户可以在“设置”中清除本机统计和诊断记录。删除设备记录会删除本机钥匙串引用，但不会自动撤销远端订阅；远端凭据仍需在对应管理后台轮换。
