# Glide 隐私说明

## 结论

Glide 不建立云端用户档案，不包含远程分析 SDK，也不会把启动事件、设备标识、诊断结果或线路配置发送给 Glide 服务器。

## 本机保存的数据

| 数据 | 保存位置 | 用途 |
| --- | --- | --- |
| 后台管理密码 | Glide 专属本机目录中的 AES-256-GCM 加密数据库 | 自动登录用户自己的管理后台 |
| 本机凭据加密密钥 | 与密文分开的 Glide 专属本机文件 | 无需电脑密码即可解密后台管理密码 |
| 临时 Cloudflare API Token | 当前创建会话的进程内存 | 只在用户确认后创建 Cloudflare 资源；取消、成功或退出即销毁 |
| 线路名称、管理地址、地区偏好、首选线路 ID 与选择理由 | 本地 SQLite | 展示、本机检查与节点选择 |
| 脱敏诊断结果 | 本地 SQLite | 排查连接问题 |

真实订阅和单节点 URI 不会写入 SQLite。它们只在用户主动优化、选择线路或选择节点后
短暂存在于应用内存中；节点列表只包含哈希标识、显示名称、协议和区域标签。用户点击
复制或显示二维码时，所选内容才会进入系统剪贴板或当前二维码窗口。诊断默认保留 7 天，
可改为 14 或 30 天，也可立即清除。

Glide 生成的订阅固定使用上游 `target=mixed` 原始格式，避免客户端 User-Agent 触发
上游的第三方订阅转换服务。需要 Clash 专用 YAML 时，当前版本不会在后台静默调用第三方
转换器；应选择支持原始 VLESS 订阅的客户端。

## 会发生的网络请求

- 用户主动检查连接或点击“一键优化”时，请求用户填写的 HTTPS 管理地址和该地址生成的订阅；
- 节点域名被标准 Fake-IP 接管或系统 DNS 失败时，Glide 依次请求阿里公共 DNS 和
  Cloudflare DNS 取得真实公网地址；只发送节点域名和记录类型，不发送管理域名、
  UUID、节点路径、端口、订阅 Token 或完整 URI；
- TLS 节点测速只向解析后的节点入口发送不含用户凭据的 `HEAD /`，并校验证书；
  测速结果只保留在当前界面，不写入本地工作区；
- 用户点击官方下载按钮时，由系统浏览器打开允许列表中的 GitHub 或 Cloudflare 官方页面；
- 用户主动验证 Cloudflare Token、预览或确认创建时，请求 Cloudflare 官方 API；
- 用户确认创建后，从锁定的 GitHub Raw 地址下载部署载荷并在任何云端写入前校验
  SHA-256；
- 当前版本不向 Glide 自有服务器发送请求。

GitHub、Cloudflare、操作系统和网络服务商可能按照各自政策处理网络元数据，这些处理不由 Glide 控制。

## 不保存的数据

- Cloudflare 账号密码；
- 电脑登录密码或系统管理员密码；
- Global API Key；
- 用户浏览历史和访问内容；
- 远程遥测标识；
- 完整订阅地址或密码的诊断日志。

## 用户控制

用户可以在“设置”中清除诊断记录，也可以在“连接”中删除对应的本机凭据引用。本机删除不会撤销远端订阅；远端凭据仍需在对应管理后台轮换。

Cloudflare Token 只存在于当前进程内存，部署成功、取消或应用退出后即销毁；这不等于
撤销 Cloudflare 服务器上的 Token。建议把 Token 有效期设为 1 天，并在不再使用时到
Cloudflare 官方控制台撤销。

本地凭据目录不会上传。macOS/Linux 使用仅当前用户可访问的目录和文件权限，Windows
继承当前用户 AppData ACL。因为解密密钥也必须由应用直接读取，所以已经取得同一用户
权限的恶意程序仍可能读取密钥和密文；FileVault/BitLocker 与系统账号安全仍然必要。
